namespace Core.Models
{
    public enum LogType
    {
        INFO,
        WARNING,
        ERROR
    }

    public enum OperationClaimEnum
    {
        admin = 0
    }

    public enum SortType
    {
        Ascending,
        Descending
    }
}
